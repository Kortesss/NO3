#include "deltawin.h"
#include "ui_deltawin.h"

deltaWin::deltaWin(QWidget *parent) :
    QMainWindow(parent),
    ui(new Ui::deltaWin)
{
    ui->setupUi(this);
}

deltaWin::~deltaWin()
{
    delete ui;
}
